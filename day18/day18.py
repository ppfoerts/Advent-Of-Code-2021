# Day 18 Snailfish
#!/usr/bin/python

import math


# the pair's left value is added to the first regular number to the left of the 
# exploding pair (if any), and the pair's right value is added to the first regular 
# number to the right of the exploding pair (if any)
def explodePair():
    x = 1

def splitPair(value):
    x = value // 2
    y = math.ceil(value / 2)
    return [x,y]

def reduceSnailFishNumber():
    # If any pair is nested inside four pairs, leftmost such pair explodes
    if():
        y = 1
    # If any regular number is 10 or greater, the leftmost such regular number splits
    elif():
        x = 1
    else:
        return

print(splitPair(5))